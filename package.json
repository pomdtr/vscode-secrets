{
	"name": "secrets",
	"publisher": "pomdtr",
	"displayName": "Secrets",
	"description": "Manage secrets in a secure way",
	"version": "0.0.1",
	"repository": {
		"type": "git",
		"url": "https://github.com/pomdtr/vscode-secrets"
	},
	"engines": {
		"vscode": "^1.68.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onStartupFinished"
	],
	"browser": "./dist/extension.js",
	"contributes": {
		"configuration": {
			"title": "Secrets",
			"properties": {
				"secrets.enabledFolders": {
					"type": "array",
					"markdownDescription": "The secrets folders to load in the current Workspace",
					"items": {
						"type": "string"
					},
					"default": [
						"default"
					],
					"scope": "window"
				}
			}
		},
		"menus": {
			"view/title": [
				{
					"command": "secrets.import",
					"when": "view == secretExplorer"
				},
				{
					"command": "secrets.export",
					"when": "view == secretExplorer"
				},
				{
					"command": "secrets.refresh",
					"when": "view == secretExplorer",
					"folder": "navigation"
				}
			],
			"view/item/context": [
				{
					"command": "folders.create",
					"when": "view == secretExplorer && viewItem == category-folders",
					"folder": "inline@1"
				},
				{
					"command": "folders.enable",
					"when": "view == secretExplorer && viewItem == disabled-folder"
				},
				{
					"command": "folders.disable",
					"when": "view == secretExplorer && viewItem == enabled-folder"
				},
				{
					"command": "secrets.create",
					"when": "view == secretExplorer && viewItem =~ /^enabled-folder$|^disabled-folder$/",
					"folder": "inline@1"
				},
				{
					"command": "folders.delete",
					"when": "view == secretExplorer && viewItem =~ /^enabled-folder$|^disabled-folder$/",
					"folder": "inline@2"
				},
				{
					"command": "secrets.delete",
					"when": "view == secretExplorer && viewItem == secret",
					"folder": "inline@3"
				},
				{
					"command": "secrets.copy",
					"when": "view == secretExplorer && viewItem == secret",
					"folder": "inline@1"
				},
				{
					"command": "secrets.edit",
					"when": "view == secretExplorer && viewItem == secret",
					"folder": "inline@2"
				}
			],
			"commandPalette": [
			]
		},
		"views": {
			"explorer": [
				{
					"id": "secretExplorer",
					"name": "Secrets",
					"contextualTitle": "Secrets",
					"icon": "$(lock)"
				}
			]
		},
		"commands": [
			{
				"command": "folders.create",
				"title": "Add Folder",
				"icon": "$(plus)",
				"category": "secrets"
			},
			{
				"command": "folders.delete",
				"title": "Add Folder",
				"icon": "$(trash)",
				"category": "secrets"
			},
			{
				"command": "folders.enable",
				"title": "Enable Folder",
				"category": "secrets"
			},
			{
				"command": "folders.disable",
				"title": "Disable Folder",
				"category": "secrets"
			},
			{
				"command": "secrets.refresh",
				"title": "Refresh Secrets",
				"icon": "$(refresh)",
				"category": "secrets"
			},
			{
				"command": "secrets.edit",
				"title": "Edit Secret",
				"shortTitle": "Edit",
				"icon": "$(pencil)",
				"category": "secrets"
			},
			{
				"command": "secrets.copy",
				"title": "Copy Secret",
				"shortTitle": "Copy",
				"icon": "$(clippy)",
				"category": "secrets"
			},
			{
				"command": "secrets.delete",
				"title": "Delete Secret",
				"shortTitle": "Delete",
				"icon": "$(trash)",
				"category": "secrets"
			},
			{
				"command": "secrets.create",
				"title": "Create Secret",
				"shortTitle": "Create",
				"icon": "$(plus)",
				"category": "secrets"
			},
			{
				"command": "secrets.import",
				"title": "Import Secrets",
				"category": "Secrets"
			},
			{
				"command": "secrets.export",
				"title": "Export Secrets",
				"category": "Secrets"
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "npm run package",
		"compile": "webpack",
		"watch": "webpack --watch",
		"package": "webpack --mode production --devtool hidden-source-map",
		"compile-tests": "tsc -p . --outDir out",
		"watch-tests": "tsc -p . -w --outDir out",
		"pretest": "npm run compile-tests && npm run compile && npm run lint",
		"lint": "eslint src --ext ts",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/glob": "^7.2.0",
		"@types/mocha": "^9.1.1",
		"@types/node": "16.x",
		"@types/vscode": "^1.68.0",
		"@typescript-eslint/eslint-plugin": "^5.27.0",
		"@typescript-eslint/parser": "^5.27.0",
		"@vscode/test-electron": "^2.1.3",
		"buffer": "^6.0.3",
		"eslint": "^8.16.0",
		"glob": "^8.0.3",
		"mocha": "^10.0.0",
		"process": "^0.11.10",
		"ts-loader": "^9.3.0",
		"typescript": "^4.7.2",
		"webpack": "^5.72.1",
		"webpack-cli": "^4.9.2"
	}
}
